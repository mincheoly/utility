from setuptools import setup, find_packages

setup(
    name="utility",
    version="1.0",
    author="Min Cheol Kim",
    description=("Min Cheol's utility functions"),
    packages=find_packages()
)